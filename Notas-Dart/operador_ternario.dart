/*
void main() {
  int a = 15;
  int b = 20;

  // Operador ternario para determinar el mayor entre a y b
  int mayor = (a > b) ? a : b;

  print("El mayor entre $a y $b es $mayor");
}
*/
/*
void main() {
  int numero = 0;

  // Operador ternario para determinar si el número es positivo, negativo o cero
  String tipoNumero = (numero > 0)
      ? "Positivo"
      : (numero < 0)
          ? "Negativo"
          : "Cero";

  print("El número $numero es $tipoNumero");
}
*/

void main() {
  int numero = 5;

  // Operador ternario para determinar si el número es par o impar
  String resultado = (numero % 2 == 0) ? "Par" : "Impar";

  print("El número $numero es $resultado");
}
